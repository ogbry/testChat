import React from 'react'

export default function LoginTitle(props) {
    return (
        <React.Fragment>
            <h2>
                {props.title}
            </h2>
        </React.Fragment>
    )
}
